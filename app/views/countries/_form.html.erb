<%= form_with(model: country, local: true) do |form| %>
  <% if country.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(country.errors.count, "error") %> prohibited this country from being saved:</h2>

      <ul>
      <% country.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :countryname %>
    <%= form.text_field :countryname, id: :country_CountryName %>
  </div>

  <div class="field">
    <%= form.label :countryabbrev %>
    <%= form.text_field :countryabbrev, id: :country_CountryAbbrev %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
