json.extract! reservation, :id, :date, :customerid, :reservestatusid, :productid, :colorid, :heelID, :Quantity, :employeeid, :created_at, :updated_at
json.url reservation_url(reservation, format: :json)
