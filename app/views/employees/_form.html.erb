<%= form_with(model: employee, local: true) do |form| %>
  <% if employee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(employee.errors.count, "error") %> prohibited this employee from being saved:</h2>

      <ul>
      <% employee.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <div class="panel panel-primary">
      <div class="panel-heading">Panel with panel-primary class</div>

      <div class="panel-body">
        <div class="row">
          <div class="col s6">
            <%= form.label 'First Name' %>
            <%= form.text_field :FirstName, id: :employee_FirstName %>
          </div>

          <div class="col s6">
            <%= form.label 'Last Name' %>
            <%= form.text_field :LastName, id: :employee_LastName %>
          </div>
        </div>

        <div class="row">
          <div class="col s6">
            <%= form.label :Phone %>
            <%= form.text_field :Phone, id: :employee_Phone %>
          </div>

          <div class="col s4">
            <%= form.label 'Employee Type' %>
            <%= form.collection_select(:EmployeeTypeID, EmployeeType.all, :id, :EmpTypeName, {prompt: "Employee Type"}, {class: "form-control"}) %>
          </div>

          <div class="col s2">
            <%= form.label 'Employee Status' %>
            <%= form.collection_select(:EmployeeStatusID, EmployeeStatus.all, :id, :StatusName, {prompt: "Status"}, {class: "form-control"}) %>
          </div>
        </div>

        <div class="actions">
          <%= form.submit class: "btn btn-primary" %>
        </div>
      </div>
    </div>

<% end %>
