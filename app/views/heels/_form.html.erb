<%= form_with(model: heel, local: true) do |form| %>
  <% if heel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(heel.errors.count, "error") %> prohibited this heel from being saved:</h2>

      <ul>
      <% heel.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :height %>
    <%= form.text_field :height, id: :heel_height %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
